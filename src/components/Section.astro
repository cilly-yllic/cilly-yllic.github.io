---
interface Props {
  title: string;
  id?: string;
}

const { title, id } = Astro.props;
---

<section id={id} class="section">
  <h2>{title}</h2>
  <slot />
</section>

<style>
  .section {
    margin-bottom: var(--space-16);
  }
</style>
